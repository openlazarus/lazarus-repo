import { Log } from '@/model/log'

export function generateMockLogs(
  _count: number = 10,
  _workspaceId?: string,
): Log[] {
  // Return empty array for now - this is just to fix build
  return []
}
