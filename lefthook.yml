pre-commit:
  parallel: false
  commands:
    lint-api:
      root: "packages/lazarus-api/"
      glob: "*.ts"
      run: npm run precommit
    lint-ui:
      root: "packages/lazarus-ui/"
      glob: "*.{js,jsx,ts,tsx,css,scss,md,json,html}"
      run: npm run precommit
